# EfficientNet-Based-LSTM-Image-Caption-Generator
This project builds an end-to-end deep learning pipeline that extracts visual features from images and generates meaningful natural language captions. It uses transfer learning, tokenization, and sequence modeling to convert images into human-readable descriptions.
